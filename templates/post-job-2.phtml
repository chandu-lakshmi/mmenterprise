<div class="container post-job">
	<div class="row">
		<div class="job-title text-center">
			<a ui-sref="^.postJob"><img src="public/images/arrow-back.svg" alt="arrowback"></a>
			<h2>Get Ready to Post Your Request</h2>
		</div>
		<div class="job-info">
			<div>
				<span class="company-name" style="margin:0">Enter Pi</span>
			</div>
			<div class="job-name">
				<div class="col-lg-4">
					<div>
						<span>Job Title:</span>
						<span>{{post2Ctrl.post_job_result.job_title}}</span>
					</div>
					<div>
						<span>Job function:</span>
						<span>{{post2Ctrl.post_job_result.job_func.job_function_name}}</span>
					</div>
					<div>
						<span>Location:</span>
						<span>{{post2Ctrl.post_job_result.location}}</span>
					</div>
					<div>
						<span>Experience:</span>
						<span>{{post2Ctrl.post_job_result.experience.experience_name}}</span>
					</div>
					<div>
						<span>Position ID:</span>
						<span>{{post2Ctrl.post_job_result.position_id}}</span>
					</div>
				</div>
				<div class="col-lg-8">
					<p style="color: #9A9A9A;font-size:16px;">Job Description:</p>
					
					<p>Responsibilities and tasks:</p>
					<p>{{post2Ctrl.post_job_result.job_desc}}</p>
				</div>
			</div>
		</div>
	</div>
</div>
<div class="container-fluid reward-ref"  style="border-top:1px solid silver;padding-top:2%">
	<h2>SELECT THE REWARD </h2>
	<div class="container" style="padding: 40px 15px">
		<div class="choose-pay row row-centered">
			<div class="col-lg-3 col-centered" ng-repeat="rewards in post2Ctrl.rewards" ng-init="radio = 'cond'+$index" >
				<label style="width:100%;margin-bottom:40px;">
					<div class="amount-pay" ng-class="{'amount-s':rewards == post2Ctrl.rewardCond}">
						<h4>{{rewards.pay_status | uppercase}}</h4>
						<h1 ng-if="$index == 0" style="visibility: hidden;">&#36; {{rewards.amount}}</h1>
						<h1 ng-if="$index != 0">&#36; {{rewards.amount}}</h1>
						<h4>{{rewards.message | uppercase}}</h4>
						<input type="radio" name="amount-ref" ng-model="post2Ctrl.rewardCond" ng-value="rewards" ng-click="post2Ctrl.payCond()">
						<label></label>
					</div>
				</label>
			</div>
		</div>
	</div>
</div>

<div class="container-fluid" style="padding-bottom: 100px;">
	<div class="container" style="border-top:1px solid #ccc">
		<h2 class="text-center" style="color: #388A73;padding: 30px 15px 40px;margin:0">SELECT THE DISTRIBUTION LIST </h2>
		<div class="row select-type row-centered">
			<div class="col-lg-3 col-centered" ng-repeat="buckets in post2Ctrl.bucket_names">
				<div class="amount-pay" ng-style="{'background':post2Ctrl.bucketColor[$index]}" ng-init="icon_cond = 'icon'+$index">
				    <img src="public/images/add.svg"  ng-click="post2Ctrl[icon_cond] = true;post2Ctrl.pushed($index+1)" ng-if="!post2Ctrl[icon_cond]">
				    <img src="public/images/select.svg"  ng-click="post2Ctrl[icon_cond] = false;post2Ctrl.spliced($index+1)" ng-if="post2Ctrl[icon_cond]">
				    <h3>{{buckets.bucket_name | uppercase}}</h3>
				</div>
			</div>
		</div>
	</div>
	<div class="text-center" style="padding: 40px 0">
		<button class="btn btn-style" ng-click="post2Ctrl.requestSuccess()" style="padding: 12px 32px;">POST REQUEST</button>
	</div>
</div>
